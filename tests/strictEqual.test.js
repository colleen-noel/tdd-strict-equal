const describe = require('mocha').describe
const it = require('mocha').it
const expect = require('chai').expect

describe('strictEqualFile', () => {
  describe('strictEqual()', () => {
    it('returns true when the values are the same', () => {
      // test data needed
      //running the code to be tested
      //assertions - expect
    })
    it('returns false when values are not the same', () => {
      // test data needed
      //running the code to be tested
      //assertions - expect
    })
    it('returns false when types are not the same', () => {
      // test data needed
      //running the code to be tested
      //assertions - expect

    })
  })
})
